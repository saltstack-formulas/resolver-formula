{% set resolver = pillar.get('resolver', {}) -%}
{% set nameservers = resolver.get('nameservers', []) -%}
{%- if 'searchpath' in resolver -%}search {{ resolver['searchpath'] }}{% endif %}
{%- for ns in nameservers %}
nameserver {{ ns }}
{%- endfor %}
